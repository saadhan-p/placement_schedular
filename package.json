{
  "name": "placement-scheduler-root",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "build": "npm --prefix frontend install && npm --prefix frontend run build && mkdir -p dist && cp -r frontend/dist/. dist/ && cp -r backend api/"
  }
}
